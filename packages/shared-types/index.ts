// Axiom shared types.
//
// Scaffolded in Slice 0 — real entity/event types (Organization, Goal, Milestone,
// Customer, Deal, Event, Task, Decision, AuditLogEntry, etc. — see docs/SDD.md §4) land
// in Slice 1 once the Prisma schema exists.

/** Placeholder export so this package builds and can be imported from Slice 0 onward. */
export type AxiomPlaceholder = {
  readonly note: "shared-types package scaffolded in Slice 0; real types land in Slice 1";
};
